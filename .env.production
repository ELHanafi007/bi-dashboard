# This URL will be updated once the backend is deployed on Render
# For now, use a placeholder — set the real URL in Vercel's Environment Variables dashboard
VITE_API_URL=https://bi-dashboard-backend.onrender.com/api
